# Ansible_handlers
Sometimes you want a task to run only when a change is made on a machine. Handlers are tasks that only run when notified.
# Dynamically installation of HTTPD
 Dynamically loaded the variable file named same as OS_name and just by using the variable names we Configured our target node.(Note: No need to use when keyword here.)
